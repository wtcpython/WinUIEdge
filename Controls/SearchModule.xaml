<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Edge.SearchModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Edge"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid ColumnSpacing="5">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ComboBox Name="SearchEngineBox" VerticalAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Image Width="16" Height="16" ToolTipService.ToolTip="{Binding Name}">
                        <Image.Source>
                            <BitmapImage UriSource="{Binding Icon}"/>
                        </Image.Source>
                    </Image>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBox Name="InputBox" Grid.Column="1" PlaceholderText="输入想要搜索的内容" KeyDown="SearchKeyDown"/>

    </Grid>
</Page>